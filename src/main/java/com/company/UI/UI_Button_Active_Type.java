package com.company.UI;

public enum UI_Button_Active_Type {
    NORMAL,
    CONNECT,
    EVENT,
    FUNCTION,
    AND_JOIN,
    OR_JOIN,
    XOR_JOIN,
    AND_SPLIT,
    OR_SPLIT,
    XOR_SPLIT,
    START_EVENT,
    END_EVENT,
    ACTIVATING_START_EVENT,
    ACTIVATING_FUNCTION
}
